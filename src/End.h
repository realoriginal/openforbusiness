#pragma once

/*!
 *
 * Purpose:
 *
 * End of code marker for the extraction script. Acts as
 * the true end of code marker for the various functions
 * using to pull various information appended at the end
 * of our payload.
 *
!*/
D_SEC( C ) __attribute__(( naked )) VOID EndOfCode( VOID );
